# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: dolphinnext-barcodeseq-1.0
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - fastqc=0.11.8
  - trimmomatic=0.39
  - fastx_toolkit=0.0.14
  - pandaseq=2.11